name: jackett
services:
    jackett:
        container_name: jackett
        restart: always
        ports:
            - 9117:9117
        volumes:
            - /docker/config:/config:rw
            - /docker/downloads:/downloads:rw
        image: linuxserver/jackett:latest
