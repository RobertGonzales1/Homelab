name: uptime-kuma
services:
    uptime-kuma:
        container_name: UptimeKuma
        restart: always
        ports:
            - 3001:3001
        volumes:
            - uptime-kuma:/app/data
        image: louislam/uptime-kuma:2
volumes:
    uptime-kuma:
        name: uptime-kuma
