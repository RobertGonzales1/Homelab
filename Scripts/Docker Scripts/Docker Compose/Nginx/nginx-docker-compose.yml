services:
  nginx:
    image: 'docker.io/jc21/nginx-proxy-manager:latest'
    restart: always
    network_mode: bridge
    ports:
      - '80:80'
      - '81:81'
      - '443:443'
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt
